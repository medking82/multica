# GA401 Multica v0.4.39 upgrade

## Outcome and authority

User requested GA401 upgrade while preserving / Skill picker and voice; user also reported public agent.hankee.com outage. Upgrade/deploy is authorized. No automatic release resume or rollback is authorized. Windows Desktop release 0.4.39-custom.26 is already complete; this task upgrades the GA401 backend/frontend and then its existing runtime CLI.

## Context and invariants

- Candidate base f42a0a4678f3aa8ecba981f542d3ef3b66996249 includes official v0.4.39 (0eff894b59b6e8e28a886290d77cb41963c7449d), reviewed custom / Skill picker and native Codex dictation. Existing Desktop tests/review and release artifact checks are retained evidence. Preserve all custom feature code byte-for-byte.
- Existing GA401 app source is 560f01203d0ce886edf9cd4270588bb20ec61176. Preserve the existing invite-only behavior from 3a08c515a3d9381aebe7942b3e4ad6224f93ffbd. Authentication transports, allowed-email configuration, account profiles and registrations are unchanged.
- Native dictation remains a Windows Desktop capability using the native adapter. GA401 provides compatible server APIs; this change does not add a paid transcription API or capture audio.
- Server Compose project multica is /home/marck/services/multica/app/docker-compose.selfhost.yml. Current backend image multica-backend-invite-only:560f01203, frontend multica-web:560f01203. Keep PostgreSQL pgvector/pgvector:pg17, volumes multica_pgdata and multica_backend_uploads, and all environment secrets.
- Runtime/browser project is /home/marck/services/multica-runtime/releases/browser-update-20260831-1/browser-update/compose.yaml. Keep runtime home, CLI/browser volumes, browser image, native account configuration and agent registrations. The candidate runtime derives from the exact old runtime image and copies only `/app/multica` to `/usr/local/bin/multica`; provider/browser tools remain owned by their existing guarded updater.
- Live ledger ends at migration 440. Official migrations 441..450 are reviewed as part of upgrade admission; 449 backfills immutable autopilot creator from publisher. There are currently zero autopilot triggers and zero queued/running agent_task_queue rows. Recheck before cutover. Issue in_progress status alone is not an executing provider process.
- Public outage is independent stale DNS origin after WAN IP changed. Its bounded one-record repair preserves Cloudflare proxy and source-restricted router DNAT. Do not alter Caddy, router, certificates or DNS as part of upgrade.

## Allowed implementation surface

Port the three existing invite-only source/test files; add bounded deploy/ga401-upgrade tooling and tests, this plan and repository-local .sop server workflow/controller. Do not change upstream migrations, shared / or voice implementation, Desktop release controller, unrelated source checkouts, JBFactory/Commerce services, providers, ingress policy, or user data.

## Phase and failure boundaries

1. Read-only preflight: exact host/projects/old images, config hashes, volumes, health, active tasks/processes and disk capacity. Discover all ownership or prerequisite failures before live mutation.
2. Build exact source commit into new immutable custom image tags without production credentials. Record image IDs, source identity and feature binding. No replacement of running containers.
3. Capture private database backup and rehearse restore plus candidate migrations in an isolated PG17 database/container without production mounts or public port. Failures leave production unchanged. Validate invite-only rules and migration completion there.
4. Before activation, recheck original config/image identities and no active work, stop only app writers and the owned runtime, capture a fresh validated consistent dump after stopping, and preserve exact compose/env state and uploads/runtime backups privately. Persist phase and exact restore references.
5. Promote backend/frontend with an explicit separate image override, keeping originals and PostgreSQL. Verify readyz/db/migrations, health source, public route, data counts and volume identity before restarting the existing runtime. Any failure stops, preserves evidence and reports whether writers are stopped; no automatic rollback, retry or ledger edits.
6. Preserve the existing runtime CLI/browser/provider configuration and verify runtime admission without provider inference. The controller upgrades the Multica binary in a derived runtime image; provider/browser updates remain separate. This one-shot controller does not enable recurring automatic updates.

Future recurring source synchronization must merge upstream with conflict resolution and pause for required review. It must not rsync source trees.

Rollback is an explicit operator action using exact old image/config references and the final consistent database/uploads/private state snapshot. Never restore an old DB after new writes without reconciling data loss. No docker down -v, volume deletion, prune, credential copying to public artifacts or new account login.

## Verification

Run focused invite-only regressions against an isolated test database, deterministic deploy-script behavior tests with fake subprocesses, feature hash/source bindings to the already verified custom release, image build, migration rehearsal and live read-after-write checks. High-risk frozen diff receives one Native Review through the installed canonical owner after deterministic checks. Canonical repository SOP owns commit/push/release. Real microphone dictation remains a user-operated smoke test; do not claim it was exercised.


<!-- sop-risk-classification: {"facts":{"blast_radius":"shared","change_kind":"schema_or_data_migration","data_boundary":"sensitive","destructive":"no","failure_cost":"material","irreversibility":"reversible","operational_controls":"not_applicable","privilege_boundary":"unchanged","project_policy":"default","rollback":"costly","scope_knowledge":"known","uncertainty":"low","verification":"deterministic"},"formal_review":"required","kind":"risk-classification-assessment","reasons":{"formal_review":["high_risk_requires_review"],"risk":["schema_or_data_migration","material_failure_with_costly_rollback"]},"risk":"high","schema_version":2} -->
